let users = []

users.push({
    id: users.length + 1,
    name:'Pablo'
})

users.push({
    id: users.length + 1,
    name:'Andres'
})

users.push({
    id: users.length + 1,
    name:'Otro Usuario'
})

users.push({
    id: users.length + 1,
    name:'Otro Usuario 2'
})


users.push({
    id: users.length + 1,
    name:'Otro Usuario 3'
})


export default users
